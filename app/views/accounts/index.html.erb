<div class="card">
  <h2>Accounts</h2>
  <p>Total Balance: <%= @total_balance %><p>
  <% @accounts.each do |account| %>
    <div class="card">
      <h3> <%= link_to account.name, account_path(account) %></h3>
      <p>Balance: <%= account.balance%></p>
    </div>
  <%end%>

  <%= link_to "+ Add Account", new_account_path, remote: true %>

</div>
